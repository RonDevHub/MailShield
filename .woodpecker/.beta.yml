pipeline:
  build-beta:
    image: plugins/docker
    settings:
      registry: ghcr.io
      repo: ghcr.io/rondevhub/mailshield
      tags: beta
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
    when:
      branch: beta