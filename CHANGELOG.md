# Changelog

### Fixed
- The CAPTCHA integration has been updated and can no longer be bypassed.

### Added
- SQLite (default, zero config), PostgreSQL (recommended for production) Switch via environment variables.

